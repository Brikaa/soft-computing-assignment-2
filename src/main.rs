struct Solution {
    chromosome: Vec<f64>,
    fitness: f64,
}

struct Point {
    x: f64,
    y: f64,
}

fn create_initialize_population(lb: f64, ub: f64, degree: i32, pop_size: i32) {}

fn main() {
    println!("Hello, world!");
}
